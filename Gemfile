source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'progress_bar'

group :development, :test do
  gem 'byebug'
  gem 'rspec'
  gem 'webmock'
  gem 'simplecov'
end
